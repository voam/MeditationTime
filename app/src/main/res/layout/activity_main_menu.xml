<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_slide_menu"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_main"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">

    <include android:id="@+id/Journey_btn"
        layout="@layout/button_main_journey"
        android:layout_width="fill_parent"
        android:layout_height="80dp"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/information"
        android:layout_marginLeft="35dp"
        android:layout_marginRight="35dp"
        android:layout_marginTop="50dp"/>

    <include android:id="@+id/Mood_btn"
        layout="@layout/button_main_mood"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_alignEnd="@+id/Journey_btn"
        android:layout_alignStart="@+id/Journey_btn"
        android:layout_below="@+id/Journey_btn"
        android:layout_marginTop="64dp"/>

    <include android:id="@+id/Bala_btn"
        layout="@layout/button_main_balancing"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_alignEnd="@+id/Mood_btn"
        android:layout_alignStart="@+id/Mood_btn"
        android:layout_below="@+id/Mood_btn"
        android:layout_marginTop="64dp"/>

    <ImageButton
        android:id="@+id/information"
        style="?android:attr/borderlessButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/icn_info_blue"
        android:layout_marginEnd="9dp"
        android:layout_alignParentEnd="true" />

</RelativeLayout>
